# AI Restore TODO

1. Open `Boss > AI Control Center`.
2. Enter provider `Base URL`, `Model`, and `API Key`.
3. Click `Save AI Settings`.
4. Click `Enable Super Mode`.
5. Run `AI Live Test` with a real system task prompt.
6. Confirm AI returns useful answer and suggestions.
7. Use AI on one real workflow:
   - Email campaign rewrite
   - Product section copy generation
   - Admin action plan
8. Keep `Current Mode` as fallback when provider is unavailable.

## Validation Commands

From project root:

```powershell
npm run build
npm --prefix backend run build
```

